{
  "name": "trivia",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Trivia configurable del ecosistema CloserClick (Vite, sin framework).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test"
  },
  "dependencies": {
    "@closerclick/closer-click-store": "^0.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "vite": "^7.3.1"
  }
}
